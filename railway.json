{
  "$schema": "https://railway.app/railway.schema.json",
  "name": "VEX Protocol",
  "description": "The trust layer for AI agents - adversarial verification, temporal memory, and cryptographic proofs.",
  "repository": "https://github.com/provnai/vex",
  "variables": {
    "VEX_JWT_SECRET": {
      "description": "A secure 32+ character string for signing JWT tokens.",
      "generator": "secret"
    },
    "DEEPSEEK_API_KEY": {
      "description": "API Key for DeepSeek. (Or use MISTRAL_API_KEY / OPENAI_API_KEY).",
      "required": false
    },
    "DATABASE_URL": {
      "description": "SQLite connection string. Point this to the persistent volume mount.",
      "default": "sqlite:///data/vex.db?mode=rwc"
    },
    "VEX_ENV": {
      "description": "Environment mode (production enforces security defaults).",
      "default": "production"
    }
  }
}
